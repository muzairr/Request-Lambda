# Request-Lambda

This Lambda function is to be embedded anywhere in the flows on AWS from where we need to send a GET request to the client CRM service which would get the data 
This links to https://github.com/muzairr/CRM-Data-Sync
 
